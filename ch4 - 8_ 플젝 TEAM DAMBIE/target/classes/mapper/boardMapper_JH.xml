<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.fastcampus.ch4.dao.BoardMapper">
    <select id="count" resultType="int">
        select count(*) from board_jh
    </select>

    <select id="selectAll" resultType="BoardDto_JH">
        select * from board_jh
        order by reg_date desc, bno desc
    </select>

    <select id="selectPage" parameterType="map" resultType="BoardDto_JH">
        select bno, title, writer, reg_date, view_cnt
        from board_jh
        order by reg_Date desc, bno desc
            limit #{offset},#{pageSize}
    </select>

    <select id="select" parameterType="int" resultType="BoardDto_JH">
        select * from board_jh
        where bno=#{bno}
    </select>

    <insert id="insert" parameterType="BoardDto_JH">
        insert into board_jh
            (title,content,writer)
        values
            (#{title},#{content},#{writer})
    </insert>

    <delete id="deleteAll">
        delete from board_jh
    </delete>

    <delete id="delete" parameterType="map">
        delete from board_jh
        where bno=#{bno} and writer=#{writer}
    </delete>

    <update id="update" parameterType="BoardDto_JH">
        update board_jh
        set title=#{title},
            content=#{content},
            up_date=now()
        where bno=#{bno}
    </update>

    <update id="increaseViewCnt" parameterType="int">
        update board_jh
        set view_cnt = view_cnt+1
        where bno=#{bno}
    </update>

    <update id="updateCommentCnt" parameterType="map">
        update board_jh
        set comment_cnt = comment_cnt+#{cnt}
        where bno=#{bno}
    </update>

</mapper>